# 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# npm Registry 地址（默认使用官方 registry）
REGISTRY=https://registry.npmjs.org

# 服务监听端口（默认 3000）
PORT=3000

# 日志级别（可选：trace, debug, info, warn, error）
RUST_LOG=byr_jsdelivr=info,tower_http=info
